# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.venv/
venv/

# 运行产物（写到用户 CWD/950-test/，不在 plugin 目录下，无需忽略）

# 用户私有数据（不入库）
skills/ops-test/inputs/           # 目标算子清单：由 scann-repo 生成，因人而异

# 用户状态文件（本地维护）
skills/scann-repo/state.json

# 白名单（依赖本地 PDF，不入库）
skills/scann-repo/whitelist_cube.md
skills/scann-repo/whitelist_vector.md
skills/scann-repo/WHITELIST_SOURCE.md

# OS
.DS_Store
Thumbs.db
